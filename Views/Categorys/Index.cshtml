@model IEnumerable<CompShop2.Models.Categorys>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategotyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CatName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategotyID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CatName)
        </td>
        <td>

            @Html.ActionLink("Edit", "Edit", new {  id=item.CategotyID }) |
            @Html.ActionLink("Details", "Details", new { id = item.CategotyID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.CategotyID })
        </td>
    </tr>
}

</table>
